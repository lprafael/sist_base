# Backend (FastAPI) + Frontend (Nginx). La base de datos es externa (local en el host).
# Uso: docker compose up -d
# Frontend: http://localhost:3002  |  API: http://localhost:8002

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: mi-playa-backend
    ports:
      - "8002:8001"
    environment:
      - PORT=8001
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
      - ./backend/logs:/app/logs
      - ./backend/static/uploads:/app/static/uploads
    restart: unless-stopped
    networks:
      - mi-playa-network
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8001/backup/ping" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: mi-playa-frontend
    ports:
      - "3002:80"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - mi-playa-network
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/" ]
      interval: 30s
      timeout: 10s
      retries: 3

  public-catalog:
    build:
      context: ./frontend/public-catalog
      dockerfile: Dockerfile
      args:
        # URL de la API vista desde el navegador (catálogo nacional MiCoche)
        VITE_REACT_APP_API_URL: /api
    container_name: mi-playa-public
    ports:
      - "3003:80"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - mi-playa-network

networks:
  mi-playa-network:
    driver: bridge
